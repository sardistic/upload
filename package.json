{
  "name": "sardrop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private, self-hosted paste-to-image-URL service.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check public/theme.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
